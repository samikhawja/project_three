import React from 'react';

// Placeholder so the app will build
const UserDashboard = () => {
    return (
        <div>
            <h1>USER DASHBOARD page placeholder</h1>
        </div>
    );
};

export default UserDashboard;