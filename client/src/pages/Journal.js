import React from 'react';

// Placeholder so the app will build
const Journal = () => {
    return (
        <div>
            <h1>Journal page placeholder</h1>
        </div>
    );
};

export default Journal;