import React from 'react';

// Placeholder so the app will build
const Home = () => {
    return (
        <div>
            <h1>HOME page placeholder</h1>
        </div>
    );
};

export default Home;